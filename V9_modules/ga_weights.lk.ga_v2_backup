// GA-managed evaluation weights for V9 (Phase M3) — included by eval.lk.
// Written by tools/ga_v2.py; do not hand-edit the values.
// Promoted by ga_v2 run 20260813_163355 (fitness 0.9590, gen 1).

global EW_DIRECT = 2
global EW_DOT = 1.2
global EW_NOVA = 1.5
global EW_SELF_HEAL = 1.4
global EW_SHIELDS = 0.6
global EW_DENIAL = 30
global EW_TAKEN = 1.8
global EW_ENEMY_HEAL = 1.2
global EW_KILL = 5000
global EW_DEATH = 9000
global EW_DROP_PENALTY = 400
global EW_APPROACH = 60
global EW_BULB_TAX = 0.35    // fraction of damage that counts when it's on a bulb
// Path A (every-turn rollout re-rank, rollout_rerank.lk): enemy-response term
// weights. Same roles as EW_TAKEN/EW_ENEMY_HEAL/EW_DEATH but scoped to the
// re-rank adjustment, so GA can tune them on per-turn signal independently
// of the veto's evalOutcome.
global EW_RR_TAKEN = 1.2
global EW_RR_HEAL = 0.8
global EW_RR_DEATH = 6000
